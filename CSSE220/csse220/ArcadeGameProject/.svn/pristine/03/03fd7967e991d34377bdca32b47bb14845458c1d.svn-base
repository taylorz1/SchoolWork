import java.awt.Color;
import java.awt.Shape;

/**
 * 
 * This interface simply handles the drawing of objects.
 * Essentially it allows us to call a draw method for most general shapes.
 *
 * @author taylorz1.
 *         Created Nov 9, 2016.
 */
public interface Drawable {
	/**
	 * Returns the fill color that should be drawn to represent this object.
	 * 
	 * @return the fill color
	 */
	Color getColor();

	/**
	 * Returns the shape the should be drawn to represent this object.
	 * 
	 * @return the shape to draw
	 */
	Shape getShape();

}
