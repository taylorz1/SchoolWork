package editortrees;

public class addPosWrapper {
		int currentPos;
		Node toReturn;
		public addPosWrapper(int currentPos, Node toReturn) {
			this.currentPos = currentPos;
			this.toReturn = toReturn;
		}
}
